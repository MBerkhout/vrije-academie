---
title: Enabled session still missing after webhook
task: SI-98
created: 2026-09-23T19:17
---

# Enabled session still missing after webhook

Alkmaar (`a049X00000PWoHpQAL`) is visible in Salesforce since 17:16 UTC. The `vaProduct__c` webhooks were skipped because the parent lookup is `ProductGroup__c` and the reader asked for `Productgroup__c`.

## Resolution

The Alkmaar session is on the PDP after a manual product-group import. The webhook reader now uses `ProductGroup__c`. That code change is local and is not deployed yet, so the next child-only webhook still skips until staging is updated.
