---
title: PDP badge from product_type plus record_type aliases
task: SI-80
created: 2026-09-22T18:45
---

# PDP badge from product_type plus record_type aliases

## Goal

Show the Salesforce activity name on the PDP (Wandeling, Reis, Workshop, Studiedag) instead of the coarse EventGroup `record_type` fallback **lezing**. Keep `RECORD_TYPES` as a coarse enum; add aliases for Salesforce names that belong in an existing bucket.

## Approach

- PDP chip: if `product_type` is reis / studiedag / wandeling / workshop, show that singular label and link to `/ons-aanbod/{slug}`. Otherwise keep `record_type` (collegereeks, lezing, vathuis).
- `mapSalesforceRecordType`: `Live_Collegereeks` → collegereeks, `Excursies_Collegereeks` → excursie, `Online_Studiedag` → studiedag.
- Backfill existing EventGroups from Medusa `product.type` so aliases apply without a full Salesforce re-import.
