# Fix tax provider for all EU countries

## Goal

Stop add-to-cart 500s for any EU shipping country. Every tax region must use `tp_system`.

## Result

Already applied on staging (SI-86 backfill). Confirmed for all 27 EU countries.

## Evidence (2026-09-22 19:29)

- All 27 `tax_region` rows: `provider_id = tp_system`, default VAT matches `eu-countries.ts`.
- Add-to-cart Art Nouveau with shipping set to each EU country: **27/27 HTTP 200**, item present.
- `seed:region` writes `tp_system` on create and backfills any region whose provider is not `tp_system`.
