# Match cart thumbnails to product tile images

Product tiles (`PlpEventCard`) crop artwork at 4:3. Cart line thumbnails were square (64×64, 80×80 on desktop), so the same photo was framed differently.

Cart thumbnails keep the current height and use a 4:3 box, with `object-cover`, so the crop matches the tile.
