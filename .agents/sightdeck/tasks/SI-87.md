---
title: Check staging revalidate secrets over SSH
task: SI-87
created: 2026-09-22T18:57
---

# Check staging revalidate secrets over SSH

## Goal

Know whether staging frontend and Medusa have the revalidate secrets set so Studio **Clear page cache** and PLP bust can work.

## Resolution / Ergebnis

Checked `stag1` on 2026-09-22 via `frontend@` / `medusa@` `v2.vrijeacademie.nl`. Secret values were not printed.

**Frontend** `/var/www/frontend/app/frontend/.env`:

- `SANITY_STUDIO_REVALIDATE_SECRET`: absent
- `SANITY_REVALIDATE_SECRET`: set (webhook HMAC)
- `REVALIDATE_SECRET`: absent
- `POST /api/revalidate/studio` route not on disk yet (checkout is `staging` @ `411209b`)

**Medusa** `/var/www/medusa/app/medusa/.env`:

- `STOREFRONT_REVALIDATE_SECRET`: absent
- `STOREFRONT_REVALIDATE_PLP_URL`: absent
- Studio cache-clear secret is not used here

**GitHub Actions:** `SANITY_STUDIO_REVALIDATE_SECRET` is not in `gh secret list`.

Studio **Clear page cache** cannot work on staging until the secret is added to frontend `.env` and GitHub, Studio is redeployed, and the frontend route is deployed.
