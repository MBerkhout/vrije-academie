---
title: Enable staging Medusa SMTP mail
task: SI-72
created: 2026-09-22T17:27
---

# Enable staging Medusa SMTP mail

## Goal

Point staging Medusa at local Postfix so OTP and cadeaubon mail actually send.

## Approach

Add `SMTP_HOST=127.0.0.1`, `SMTP_PORT=25`, `SMTP_SECURE=false`, `SMTP_FROM=noreply@vrijeacademie.nl` to `~/app/medusa/.env` if missing. Reload PM2. Confirm the process has the vars and the SMTP notification provider is registered.

## Shipped

Staging `~/app/medusa/.env` now has those four vars (no SMTP auth, no SendGrid). PM2 cluster reloaded; all three workers online. DB: `smtp` enabled, leftover `local` disabled. Postfix accepted a local smoke send to `medusa@localhost` (`status=sent`). External inbox delivery not tested. Cluster log: `Notification provider with id: smtp, already exists` on extra workers — provider is still registered.
