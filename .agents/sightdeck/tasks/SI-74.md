---
title: Check if staging OTP mail was sent
task: SI-74
created: 2026-09-22T17:31
---

# Check if staging OTP mail was sent

## Goal

See whether the OTP the user requested actually went out via Medusa/Postfix.

## Finding

Not sent. OTP for `mick@thedigitalimprover.nl` was created at 17:31:02 and Medusa tried the `smtp` provider, but nodemailer aborted STARTTLS on Postfix’s self-signed cert (`self-signed certificate`). Notification `noti_01M34VVTRT7N1PS3SRT622XP9E` is `failure`. Postfix: `NOQUEUE: lost connection after STARTTLS`. HTTP `/store/auth/otp/request` still returned 200; code was only logged on the server.
