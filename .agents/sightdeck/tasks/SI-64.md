---
title: Fix medusa build TypeScript errors
task: SI-64
created: 2026-09-22T08:46
---

# Fix medusa build TypeScript errors

`medusa build` fails on deploy with five TypeScript errors:

1. `thrownError` does not exist on `WorkflowResult` in `ensure-salesforce-customer.ts` and `ensure-order-customer-salesforce-step.ts`.
2. `null` in mapped Salesforce id patches cannot be assigned to `SalesforceIdPatch[]` in `backfill-medusa-ids.ts`.
3. `Record<string, unknown> | null` assigned to `Record<string, unknown> | undefined` in `search/service.ts`.
4. `CustomerDTO[]` has no `id` in `apply-customer-from-salesforce-step.ts` (`byEmail?.id`).
