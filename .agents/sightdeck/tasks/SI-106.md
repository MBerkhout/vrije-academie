---
title: Checkout button: Betaal € amount
task: SI-106
created: 2026-09-24T19:32
---

The payment CTA in `CheckoutPaymentForm` currently reads `Betalen — € …` (`payLabel` plus an em dash and `formatPriceEur`).

Show `Betaal` plus the formatted amount when the cart total is above zero. When the total is €0 (`isFreeCheckout`), show `Bestelling plaatsen`. The busy states stay `Betaling starten…` and `Bestelling plaatsen…`.
