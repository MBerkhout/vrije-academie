# Set tp_system on all staging tax regions

## Goal

Stop add-to-cart 500s for non-NL shipping addresses by assigning the system tax provider to every EU tax region on staging.

## Change

- Staging SQL: `provider_id = tp_system` on 26 regions (all 27 EU countries now).
- Smoke test: create cart, set shipping `be`, add VA Thuis variant → 200 (was 500).
- `seed:region` writes `tp_system` on create and backfills existing regions.
