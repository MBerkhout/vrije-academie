---
title: Trace Salesforce Account text change through Medusa, Sanity, and page invalidation
task: SI-39
created: 2026-09-21T16:25
---

# Trace Salesforce Account text change

Salesforce Account `0019X00002CC7rAQAT` (ACC sandbox) had its text changed last week. Check whether that change reached Medusa and Sanity, and whether the right storefront pages were invalidated.

## Pipeline (code)

Account webhooks resolve to `docent` or `customer` via sync state, then RecordTypeId. Docent pull maps `Web_Body__c` (HTML stripped) or `Description` → Medusa `bio`, then `people.docent.updated` mirrors to Sanity. Listing/PDP cache bust only listens to product/order events, not docent updates. Sanity revalidate webhook only handles `page`, `generalSettings`, `menu` — not `docent`.

## Staging checks

1. Webhook queue rows for this Account id (status, processed_at).
2. Medusa `salesforce_sync_state` + docent/customer row (bio/text vs last week).
3. Sanity `docent` mirror `_updatedAt` and bio.
4. Whether related PDP / listing caches were busted.

## Resolution / Ergebnis

The Account is teacher **Peter van Duinen** (`Web_Body__c` = `<p>Blablabla, tekst over Peter</p>`).

**Reached Medusa and Sanity.** Thursday 18 Sep 2026 13:04:44 CEST an `Account` update webhook was received, resolved as `docent`, and marked `done` in 0.8s. Medusa `docent` `01KZDHM9STBMXWD1DFSC740F6W` and Sanity `medusa-docent-01KZDHM9STBMXWD1DFSC740F6W` both got bio `Blablabla, tekst over Peter` at 13:04:45 CEST. Live Salesforce still matches.

**Did not invalidate listing/PDP caches, and did not update search.** The only linked product is VA Thuis `leukste-vathuis-lezing3`; that product row was not touched (last Medusa product update 14 Sep). Docent updates do not fire `product.updated`, so Redis event-detail / listing snapshots and `POST /api/revalidate/plp` are not busted. The Sanity revalidate webhook ignores `_type=docent`. OpenSearch still has this docent with `body`/`excerpt` null, so `/zoeken?q=Blablabla` is empty — the search subscriber reads `bio` from the event payload (`{ id }` only), unlike the Sanity subscriber which reloads the row.

The VA Thuis PDP is `force-dynamic` and already shows the updated name/role/photo. That block does not render `bio`. Salesforce `LastModifiedDate` is 30 minutes after the only webhook; current `Web_Body__c` still matches.
