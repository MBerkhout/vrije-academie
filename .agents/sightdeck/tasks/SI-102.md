# Stop adding VAT on inclusive Salesforce prices

Salesforce `Price__c` (€26.50) already includes VAT. Cart refetches omitted `items.is_tax_inclusive`, so Medusa treated that gross price as net and added the product-type rate. The inflated total was stored as the gift-card credit and pushed to Salesforce as the voucher line.
