# Stop doubled gift card on cart refresh

The cart page loads the cart from the header, the cart view, and the checkout button at the same time. Each load re-applies the cadeaubon. When those overlap, the credit is booked twice and Producten shows about double the price until the next refresh strips it back to one line.
