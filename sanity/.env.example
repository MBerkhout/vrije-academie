SANITY_STUDIO_PROJECT_ID=your_sanity_project_id
SANITY_STUDIO_DATASET=production
# Optional custom subdomain for hosted Studio (defaults to SANITY_STUDIO_PROJECT_ID)
# SANITY_STUDIO_HOSTNAME=my-custom-name
# Hosted Studio default: https://v2.vrijeacademie.nl
# For local dev, override in .env.local: SANITY_STUDIO_PREVIEW_URL=http://localhost:3000
SANITY_STUDIO_PREVIEW_URL=https://v2.vrijeacademie.nl
# Medusa Admin base URL for "Open in Medusa" (required for the action; no code default)
SANITY_STUDIO_MEDUSA_ADMIN_URL=http://localhost:9000
# Deploy Studio + schema (sanity deploy / schema:deploy). Needs Deploy Studio permission.
# Do not reuse the content write token unless it also has deploy grants.
# SANITY_AUTH_TOKEN=
# Content API write token (scripts, migrations, MCP). Editor/content scope only.
# SANITY_API_WRITE_TOKEN=
